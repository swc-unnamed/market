model Entity {
  id                  String               @id @default(cuid())
  name                String
  type                EntityType
  uid                 String               @unique
  apiLink             String               @map("api_link")
  assets              Asset[]
  auctionListingItems AuctionListingItem[]

  @@map("entities")
}
